// Route file — delegates to the shared LoginForm component
// so it can be used both as a Next.js page (/auth/login)
// and imported directly as a component in app/page.tsx
export { default } from "@/components/auth/LoginForm";
